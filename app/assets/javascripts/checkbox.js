$(function() {
  let password = "#user_password";
  let passwordconfirmation = "#user_password_confirmation"
  let passcheck = "#check";

  (passcheck).change(function() {
    if ($(this).prop("checked")) {
      
    }
  })
});